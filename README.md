# angularjs-alura
Instalando o NodeJS - Linux (Ubuntu):
$ sudo apt-get install -y nodejs

Baixar as dependências do projeto:
Na pasta principal:
$ npm install

Subir servidor:
$ nodejs server

http://localhost:3000
